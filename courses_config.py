from course import Course

def build_course(name,lecture,section_type):

def query_courses():
    # for enroll

def run_courses_config():
    courses = []
    num = input("Number of courses to config (1,2,3, etc.): ")
    for _ in range(num):
        name = input("Name of course (MATH2D, I&CS31, etc.): ")
        lecture = input("Lecture code of course: ")
        section_type = input("W")
